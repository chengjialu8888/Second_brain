# Ideas Resolver

Use `ideas/` for user-originated raw ideas and possibilities.

Agents should not silently rewrite files here. Suggest edits or move an idea into `projects/` only when work starts or the user asks.
