# Resources Resolver

Use `resources/` for useful links, repos, articles, documents, and references as resources.

Distilled ideas from a resource should live in `concepts/` or `projects/`, with a source ref back to the resource.
